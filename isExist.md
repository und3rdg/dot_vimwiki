## check if element exist

do anything with el and catch err        

```
    el => {
        try {
            const x = el // anything!
            return true
        } catch {
            return false
        }
    }
```
